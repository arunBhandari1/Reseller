<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.reseller">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".splach_screen"
            android:label="Reseller"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Testing"
            android:label="RESELLER"
            />
        <activity android:name=".redundant.summary" />
        <activity android:name=".redundant.sold_sneakers" />
        <activity
            android:name=".AddEditShoeActivity"
            android:label="@string/title_activity_new_shoe"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".redundant.inStock"
            android:label="@string/title_activity_in_stock"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".createAccount"
            android:label="@string/title_activity_create_account"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
    </application>

</manifest>